# Source
source 'https://rubygems.org'

# Generic gems, all environments
gem 'sinatra'
gem 'sinatra-contrib'
gem 'puma'
gem 'encrypted_cookie' # Use instead default sinatra session
gem 'rest-client'
gem 'json'

# Gems for development environment
group :development do
  gem 'tux'
end

# Gems for testing environment.
group :test do
  gem 'cucumber'
end


# Gems for testing and development envoronment
#group :development, :test do
#end

# Gem for production environment
#group :production do
#end
